module.exports = {
  default: {
    red: (str) => str,
    green: (str) => str,
    blue: (str) => str,
    yellow: (str) => str,
    cyan: (str) => str,
    gray: (str) => str,
    bold: (str) => str,
    dim: (str) => str
  },
  red: (str) => str,
  green: (str) => str,
  blue: (str) => str,
  yellow: (str) => str,
  cyan: (str) => str,
  gray: (str) => str,
  bold: (str) => str,
  dim: (str) => str
};