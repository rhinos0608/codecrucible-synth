# DEPRECATED - Configuration moved to unified-config.yaml
# This file is kept for backward compatibility but should not be modified.
# All model configuration is now managed through the main unified-config.yaml
# which includes:
# - Sliding adaptive context windows (131K max)
# - Memory-aware model selection
# - Robust JSON parsing
# - Consolidated configuration management
#
# To modify model settings, edit: config/unified-config.yaml
#
# This configuration is automatically overridden by unified-config.yaml

# Minimal compatibility configuration
llm:
  # Redirect to unified config - DO NOT MODIFY
  _config_source: "unified-config.yaml"
  _deprecated: true
  _message: "Use config/unified-config.yaml for all model configuration"
  
  # Basic fallback settings (overridden by unified config)
  default_provider: "ollama"
  fallback_chain: ["ollama"]
  
  providers:
    ollama:
      endpoint: "http://localhost:11434"
      models:
        preferred:
          - "llama3.1:8b"  # Will use settings from unified-config.yaml
        fallback:
          - "qwen2.5-coder:3b"
      
  # All other settings come from unified-config.yaml
  routing:
    strategy: "hybrid"
  monitoring:
    enabled: true
  security:
    validate_inputs: true
  caching:
    enabled: true